# Progetto d'esame B031278 Deep Learning (UNIFI, Fall 2025).
# Risultati prodotti su Windows 11, Python 3.12.4, RTX 5050 Laptop
# (Blackwell, sm_120, 8 GB).
#
# Installazione in due passi, l'ordine conta:
#
#   pip install torch==2.9.1+cu128 torchaudio==2.9.1 \
#       --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# requirements.lock.txt fissa le versioni esatte dei run archiviati.

# Le wheel di default sono compilate fino a sm_90 e su Blackwell falliscono a
# runtime con "no kernel image is available for execution on the device".
# torchaudio e' pinnato senza suffisso locale di proposito, cosi' pip accetta
# sia la variante +cpu sia la +cu128.
torch==2.9.1+cu128
torchaudio==2.9.1

# torchaudio 2.9+ ha dismesso l'I/O interno: senza un backend non si caricano
# i .wav di Speech Commands.
soundfile>=0.12.1,<1.0

numpy>=2.1,<3.0
einops>=0.8,<1.0                # reshape del multi-head attention
PyYAML>=6.0,<7.0                # i file in configs/
tensorboard>=2.17,<3.0          # curve durante il run; il CSV per epoca lo
                                # scrive src/tracking.py senza dipendenze
tqdm>=4.66,<5.0                 # avanzamento del training

# Il contatore di FLOPs non ha dipendenze: e' torch.utils.flop_counter,
# nativo in torch.
